process {
    executor = 'slurm'       // Use SLURM cluster scheduler
    queue = 'galaxy'    // your GPU queue or partition
    cpus = 1                 // number of CPUs per task
    memory = '10GB'
    time = '2h'

    container = '/gladstone/finkbeiner/steve/work/projects/datastudy/nextflow-cluster.sif'  
    //containerEngine = 'singularity'
    containerOptions = "--bind /gladstone/finkbeiner:/gladstone/finkbeiner:rw \
                        --nv"

    //containerOptions = "--bind $HOME/datastudy/bin:/app \
      //                  --bind /gladstone/finkbeiner:/gladstone/finkbeiner:rw \
        //                --nv"
    //containerOptions = '--nv --output=slurm-%j.out --gres=gpu:v100:1  --no-home --cleanenv --bind /gladstone/finkbeiner/imaging-home/vgramas/datastudy/bin:/app'
    shell = ['/bin/bash']

}


